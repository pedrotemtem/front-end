import React, {Component} from "react";

export default class detections extends Component {
    render() {
        return (
            <div>Detections Page. Analyst ID: {this.props.analystID}</div>
        )
    }
}